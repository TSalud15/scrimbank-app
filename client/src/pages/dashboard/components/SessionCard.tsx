type SessionCardProps = {
    name: string;
    date: Date;
    isLoading: boolean;
};

const SessionCard = ({ name, date, isLoading }: SessionCardProps) => {
    return <div>SessionCard</div>;
};
export default SessionCard;
